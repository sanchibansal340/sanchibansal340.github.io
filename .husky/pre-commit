STAGED_FILES=$(git diff --cached --name-only)

if [ "$STAGED_FILES" != "" ]; then
echo -e "\n\n🎯 Running Prettier..."
echo "$STAGED_FILES" | xargs yarn run pretty --write
echo -e "\n\n✅ Done. Committing now..."
echo "$STAGED_FILES" | xargs git add

fi
